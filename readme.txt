1. the pub_year was restricted to 2000-2020
2. q1: produces total counts of pubs per country per database with and without doi during 2000-2020 (t1)
       produces a pivot table showing the pub intersections per database per country (t2)
3. q2: produces the raw dataset before refining and pivoting (t3)
4. q3: produces the main dataset
5. q4: produces the table with citation counts of pubs per database per country
6. q5: produces the table with publication counts per database per country per year
7. q6: sums up the total citations per country per database
8. q7: gives the database intersection combinations per country
9. q8: creates the table (t7) containg the distinct publications from the 6 countries in the four databases
10. q9: counts the distinct intersections of the database pairs for the 6 countries collectively
11. q10: finds the unique coverage and intersection combinations per country for databases
12. q11: calculates the total publications per subject per database for the countries set
13. q12: calculates the total publications by institution per database for the countries set
14. q13: calculates the total citations by institution per database for the countries set
       
       